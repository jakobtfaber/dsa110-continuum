.aider*

# Data products — never commit large binary/data files
ms/
products/
*.ms
*.fits
*.h5
*.hdf5

# Cache and scratch
.cache/
.mypy_cache/
__pycache__/
.pytest_tmp/

# Node.js (MCP server tooling)
node_modules/
package.json
package-lock.json

# Quarto render output (source .qmd files are tracked, not the built site)
docs/quarto/_site/
docs/quarto/.quarto/

#Ignore vscode AI rules
.github/instructions/codacy.instructions.md


# IDE and AI tool configs (local to each developer)
.agent/
.agents/
.augment/
.claude/skills/
.continue/
.cursor/
.factory/
.idea/
.vscode/
.windsurf/
.windsurfrules
*.log
# Python packaging artifacts
*.egg-info/

# Pipeline outputs — all working outputs are regenerable; canonical figures live in docs/images/
pipeline_outputs/
pipeline_diagnostic.png

# Forced photometry benchmark JSONs (regenerable via scripts/bench_forced_photometry.py)
outputs/photometry-bench/

# Playwright CLI cache (skill scratch state)
.playwright-cli/

# Codacy CLI local config + logs
.codacy/

# /graphify skill output (regenerable knowledge-graph artifacts)
graphify-out/
