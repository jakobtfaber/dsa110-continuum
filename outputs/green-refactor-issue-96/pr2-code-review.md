**Findings**

- **High**: Post-deploy smoke will fail whenever `h17` is disabled or replaced by a placeholder. The matrix only includes `h17` when `MONITOR_H17_ENABLED=true` ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:90)), but `all_slugs` always includes `h17` ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:95)), and placeholders intentionally omit `manifest.json` ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:306)). The smoke job still unconditionally curls `h17-live-monitor/manifest.json` ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:391)), so main deployments will fail in the default disabled-H17 configuration.

- **Medium**: Failed-leg placeholder handling does not work if no `monitor-*` artifact exists. `aggregate_site` says failed host legs should render placeholders ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:246)), but `actions/download-artifact` runs before the placeholder loop ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:267)). If the only enabled leg, `dsacamera`, fails or is canceled before upload, there is no matching artifact, so aggregation fails before lines 287-304 can create the placeholder.

- **Medium**: The added workflow-contract test is not exercised by current CI. `tests/test_docs_monitor_workflow.py` defines the service-level checks ([test_docs_monitor_workflow.py](/data/dsa110-continuum-worktrees/agent-monitor-recovery/tests/test_docs_monitor_workflow.py:30)), but `docs.yml` PR validation only runs `tools/dsacamera-monitor/tests` ([docs.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/docs.yml:57)), and the root cloud-safe subset enumerates specific tests without this file ([python-tests.yml](/data/dsa110-continuum-worktrees/agent-monitor-recovery/.github/workflows/python-tests.yml:100)). This means future workflow regressions can pass required CI despite the new test file.

I did not modify files or run tests.