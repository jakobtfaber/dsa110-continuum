Found one actionable correctness issue:

- [scripts/batch_pipeline.py:1946](/data/dsa110-continuum-worktrees/agent-sault-weighted-coadd/scripts/batch_pipeline.py:1946) still deletes the archived product mosaic and weight before rebuilding. That means the new rollback logic in `_archive_epoch_products()` cannot preserve the existing archived pair on the normal rebuild/force path, because by the time archiving runs there is no destination backup to link. If the second `os.replace()` then fails, the helper rolls back to “missing product” rather than the old valid pair. The regression test covers `_archive_epoch_products()` with an existing destination pair, but not the real caller path that removes that pair first. The product archive paths should be left in place during rebuild and replaced only by `_archive_epoch_products()` after QA passes.